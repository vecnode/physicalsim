{
  "name": "physicalsim-web",
  "private": true,
  "type": "module",
  "workspaces": [
    "common",
    "adapters/*",
    "shell"
  ],
  "scripts": {
    "build": "npm run build --workspace=shell",
    "dev": "npm run dev --workspace=shell",
    "typecheck": "tsc -p common && tsc -p adapters/rp2040 && tsc -p adapters/avr8 && tsc -p shell"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.7.3"
  }
}
